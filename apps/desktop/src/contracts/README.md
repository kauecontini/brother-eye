# Desktop contracts

Tipos do frontend serão gerados ou consumidos a partir de `schemas/`.

JSON Schema Draft 2020-12 é a source of truth.

Não duplicar domain enums manualmente em TypeScript sem teste de compatibilidade.
